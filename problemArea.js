const base_rec=3, height_rec=4, base_tr=4, height_tr=6;
if (base_rec*height_rec > (base_tr*height_tr)/2)
    console.log("The rectangle has a bigger area");
else if (base_rec*height_rec < (base_tr*height_tr)/2)
    console.log("The triangle has a bigger area");
else 
    console.log("The areas of the shapes are equal");


const hi = 8

if (hi < 9) {

} else if (hi > 9) {
        // alkjshdf
        // asldkjfh
    }
